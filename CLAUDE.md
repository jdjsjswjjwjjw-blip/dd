# CLAUDE.md — QuantSystem

> ملف السياق الدائم لمشروع QuantSystem. يُحمَّل في كل جلسة Claude Code.
> القواعد العامة للتداول الكمّي في skill `quant-rigor-guard`؛ الكود الجاهز في
> `quant-exec-kit`. **هذا الملف لخصوصيات هذا المشروع تحديداً** — اقرأه أولاً.

---

## ما هو هذا المشروع

نظام تداول كمّي على بيانات tick-level للعقود الآجلة. الهدف الحالي **ليس** نظام تداول
مستقلّ، بل **مساعد قراءة دفتر الأوامر** (النظام 2): المحلّل يقف عند مستوى سعري يختاره،
ويسأل النظام عن حالة العمق (سيطرة، امتصاص، جدران، iceberg)، والنظام **يصف** والمحلّل
**يقرّر**. الكاشف المستقلّ (النظام 1) مؤجَّل حتى يثبت الأساس.

**التداول احتمالي لا حتمي.** النظام يصف الحالة (ثقة عالية) ويعطي احتمالاً معايَراً لما
تبع النمط تاريخياً — **لا يجزم بالاتجاه أبداً**. افصل دائماً «ما أرى» عن «ما قد يحدث».

---

## القاعدة الأولى المطلقة: التسرّب هو القاتل الصامت

كل feature يُحسب من الحاضر فقط. **الـ label وحده يرى المستقبل.** أي خرق لهذا يجعل
الباك‌تست كذبة جميلة تنهار حيّاً. قبل أي feature/label/تطبيع، تحقّق من السببية.

علامات التحذير الإلزامية:
- لا إحصاءات عالمية (mean/std/min/max على السلسلة الكاملة) — استعمل نوافذ متحرّكة من
  الماضي فقط.
- فصل **وقت الحدوث** عن **وقت التأكيد**: استعمل `confirmation_index`، لا `bar_index`.
- التطبيع المُلائَم على التدريب يُطبَّق على الاختبار، لا يُعاد ملاءمته.
- لو بدا الباك‌تست **أفضل مما يبرّره الـ IC** (≈0.10)، **اشكّ في التسرّب أولاً** — لا تحتفل.

---

## مصدر الحقيقة لحدود الجلسات (مشكلة C1 — حرجة)

حدود الجلسات **متناقضة** عبر 3 ملفات. المصدر الوحيد للحقيقة:

```
✅ session_features.py  ← المرجع الوحيد (asia 00-08, london 07-16, ny 13-22, overlap 13-16)
❌ seasonal_map.py       ← متناقض (london 07-16, ny close 22) + آسيا غائبة (M2)
❌ prepare_day_trading.py daytrade_default ← متناقض (london 07-12, ny 13:30-20)
```

عند أي عمل على الجلسات: **وحّد على `session_features.py`**. التناقض يلوّث
`time_to_ny_close_min` (ميزة STRONG في تدقيق الـ IC). لا تنشئ تعريفاً رابعاً.

---

## معمارية الفرعين — أين يذهب كل شيء

```
day_trade (شمعة 5د)  →  الهيكل + التدفّق منخفض الأبعاد  →  الأحداث + labels
SSL/CNN/LSTM         →  العمق الخام (MBP-10 tensor)     →  قراءات وصفية
                        يُدمجان late-fusion (لا end-to-end بنفس الوتيرة)
```

**SSL يأتي أولاً (pre-training)، لا أخيراً.** يتعلّم بنية الدفتر الكاملة (إخفاء+تنبّؤ)،
ثم CNN/LSTM يبنيان على الـ encoder المُدرَّب. لا تدرّب العمق والهيكل من الصفر بنفس
الوتيرة — العمق عالي الأبعاد يطغى على الهيكل الرقيق (IC≈0.10).

**SSL pretext:** أخفِ دفتر الأوامر الخام + انعكاس swing حقيقي من السعر. **لا تجعل SSL
يتنبّأ بمخرَج الدورة المحسوب** (`cycle_phase_*`) — تقليد قاعدة = بلا قيمة.

---

## مصادر البيانات حسب الميزة (مشكلة D3)

| الغرض | المصدر | لا تفعل |
|---|---|---|
| الموسمية (21 ميزة) | timestamp فقط | لا تبنها من MBO |
| الدورة البنيوية (7) | OHLCV/شموع جاهزة | لا تبنها من MBO |
| العمق المكاني | **MBP-10** (10 مستويات) | BBO لا يكفي (مستوى واحد) |
| iceberg/queue/spoofing | MBO (مستوى الأمر) | — |
| tick_size/تدوير العقود | Definition | ليس feature تدريب |
| OI/settlement | Statistics | settlement = تسرّب لو intraday |

اخترنا MBO (نحتاج iceberg) → **تحقّق من إعادة بناء الكتاب بمقارنته بـ MBP-10 قبل أي
تدريب** (مشكلة D4). تجنّب مسار `build_rolling_lob_tensors_mbo_only` (قنوات فقيرة، C4).

---

## تقسيم الميزات (130 ميزة)

- **تبقى في day_trade (~105):** المستويات (أقوى عائلة)، التدفّق micro، الموسمية، الدورة
  البنيوية، الزخم الكلاسيكي. القوية فردياً: `london_sess_high`, `current_vwap`,
  `hawkes_intrabar_sum`, `tick_count`, `time_to_ny_close_min`.
- **تنتقل للعمق الخام (~20) — احذفها من parquet (مشكلة D1):** كل الجدران اليدوية +
  مستوى الأمر (absorption, spoofing, cancel_ratio, iceberg, liquidity_*).
- **مكرّرات (احذف واحداً من كل زوج):** `cvd_cumulative`=`cvd`, `obi_net`=`obi`,
  `spoofing_duration`≈`cancel_ratio` (IC متطابق).

**مهمّ:** العمق ومستوى-الأمر = **NOISE فردياً** في الـ IC (iceberg/absorption = 0.000).
هذا **لا** يعني بلا قيمة — قيمتها مكانية تفاعلية يلتقطها النموذج العميق لا الـ IC الأحادي.
لهذا تنتقل للتنسور الخام، لا تُحذف نهائياً.

---

## السؤال الوجودي غير المحسوم

iceberg و absorption = **0.000 IC فردياً**. قبل بناء عمق فوقهما، **شغّل المحاكيات**:
`modules/features_v2/iceberg_simulator.py` و `absorption_simulator.py` (في tests/، أدوات
تحقّق لا توليد بيانات). تجيب: هل الإشارة موجودة وكاشفاتك تلتقطها، أم النمط غائب؟
نجاحها على البيانات الاصطناعية **شرط ضروري لا كافٍ** — يجب النجاح على البيانات الحقيقية.

---

## القراءات الوصفية الـ 12 (مرتّبة بالأولوية)

حاسمة: امتصاص · جدران حقيقية/وهمية · **علامة البطلان (متى أنا مخطئ)**.
مؤكِّدة: ميل الدفتر+انقلابه · خريطة سيولة فوق/تحت · سياق الوصول (اندفاع/زحف+لمسة).
داعمة: iceberg · سرعة الأوامر · قيعان/قمم.
شاملة: **مرجع نسبي سببي** («Xضعف المعتاد» لا مطلق) · توافق متعدّد الأطر · مراقبة مستمرة.

تُركَّب في **أطروحة** (دخول+هدف+بطلان)، لا قراءات منفصلة. كل «قوي/هشّ» يحتاج مرجعاً
نسبياً سببياً (متوسط متحرّك من الماضي)، وإلا فهو رقم بلا معنى.

---

## التطبيع: نسبي وسببي (مشكلة C3 — حرجة للقراءات)

قراءات «جدار قوي»، «حجم البيع أقوى» كلها **مقارنات**. التطبيع يجب أن يكون:
- **نسبياً** (مقابل المعتاد، لا أرقام مطلقة).
- **سببياً** (المعتاد من الماضي فقط، لا إحصاءات تشمل المستقبل).
- افحص `_normalize_lob_tensor_nonflat` بحثاً عن إحصاءات عالمية (مثل بَق `effort_result`
  الذي رأيناه وأُصلِح).

القراءات المكانية (قيعان مقابل قمم، خريطة سيولة) تحتاج تنسور MBP-10 **كامل**.

---

## النقاط السليمة المؤكَّدة (لا تكسرها)

- إيدج «قمة لندن» حقيقي (IC≈−0.10, p≈10⁻¹⁹) — ارتداد بنيوي سببي، ليس تسرّباً.
- `detect_swings` نموذجي (يفصل bar_index عن confirmation_index) — حافظ على النمط.
- كود `ic_audit.py` نظيف (forward return سببي، كاشف تسرّب، FDR) — استعمله مرجعاً.
- محاكيا iceberg/absorption مصمّمان ضد الدورانية — نقطة قوة، شغّلهما.

---

## أوامر سلوكية لـ Claude Code

1. قبل أي feature/label/باك‌تست: طبّق `quant-rigor-guard` (اذكر القواعد المنطبقة).
2. للكود الكمّي الحسّاس (CV, Sharpe, تطبيع): استعمل `quant-exec-kit` (لا تكتب نسخة مبسّطة).
3. لا تفترض الربحية أبداً. عبء الإثبات على الإشارة.
4. حالة واحدة ناجحة ليست دليلاً — الإثبات بالتكرار (مئات الحالات، out-of-sample).
5. عند الشكّ في السببية: توقّف واسأل، لا تكتب كوداً قد يسرّب.
6. لا تخلط الـ label بالـ gate/الدورة/regime. الـ label = نتيجة سعر نقية.
